# AstrBot React Dashboard

这是 AstrBot Dashboard 的 React 迁移目录。当前阶段由 React/Vite 提供新版入口，并同源承载构建在 `public/legacy` 下的 Vue Dashboard，以确保迁移期间现有页面、交互和样式保持不变。

请从仓库根目录使用统一命令启动或构建，详见 [`../docs/dashboard-react-migration.md`](../docs/dashboard-react-migration.md)。
